# Design System

<!-- SCAFFOLD: stub, only relevant for projects with a UI. If this project has no UI (a CLI,
library, or service), delete this file and remove its references from CLAUDE.md instead of
filling it in. -->

TODO — color/type/spacing tokens, component and interaction-state patterns, motion and icon
policy. Governs how things look and behave; never overrides a feature spec's functional
scenarios.
