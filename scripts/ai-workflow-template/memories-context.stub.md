# {{REPO_NAME}} — core context

<!-- SCAFFOLD: stub. This is the ~3KB "core packet" every /sdd subagent reads once per session,
instead of the full REFERENCE.md/OVERVIEW.md/design.md. Keep it short — link out to those files
for anything long-form. Add memories/repo/slices/<layer>.md files (1.5-2.5KB each) for
architecture-layer-specific detail a subagent only needs when its task's [Layer] tag matches;
see the source project's memories/repo/slices/ for the pattern (it splits by data/domain/route/
types/ui for a Next.js App Router app — pick layer names that match this repo's own
architecture instead of copying those verbatim). -->

## Stack

TODO — one-paragraph summary; see REFERENCE.md §1 for detail.

## Commands

TODO — the two or three commands used on every task (typecheck, test, lint); see REFERENCE.md §2.

## Conventions

TODO — the handful of repo-wide conventions every task must follow regardless of layer.

## Behavioral gate

TODO — one line naming the tool and spec directory; see REFERENCE.md §6 / repo-profile.md.
