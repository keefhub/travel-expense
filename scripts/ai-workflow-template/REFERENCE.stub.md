# REFERENCE

Orientation map for this repo's AI implementation pipeline. Read before searching the repo;
update whenever something below changes.

<!-- SCAFFOLD: this whole file is a stub. Fill each section in for real, or run whatever this
repo's equivalent of an /init pass is, before starting feature work. -->

## 1. Stack

TODO — language(s), framework(s), package manager, runtime version(s).

## 2. Commands

TODO — install, dev server, build, test, lint, typecheck. State the exact command and its
success/failure signal for each.

## 3. File layout

TODO — top-level directories and what lives in each.

## 4. Framework gotchas

TODO — anything about this stack's current version that diverges from an AI model's training
data (breaking changes, renamed APIs, changed conventions), with a pointer to the authoritative
doc (vendored docs directory, changelog, migration guide).

## 5. Domain facts

TODO — facts that span multiple features and would otherwise be re-discovered/re-explained in
every feature file: core entities, invariants, terminology.

## 6. Behavioral gate

TODO — which tool verifies end-to-end/UI behavior that a compiler or type-checker can't see
(Playwright, Cypress, system tests, etc.), and where its specs live.

## 7. Feature implementation order

TODO — once features/ exists, the build order and rationale (see CLAUDE.md § Implementation
order for the table this should match).
